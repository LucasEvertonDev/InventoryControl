@model ConsultarServicosViewModel

<div class="card mb-12">
     <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="FormTitle col-12">Consulta de Serviços</h5>
    </div>
     <div class="card-body">
        <form asp-action="index">
            <div class="row form-horizontal">
              <div class="col-4 col-xs-12 mb-3">
                    <label asp-for="Nome" class="control-label">NOME</label>
                    <input asp-for="Nome" class="form-control" />
                </div>

                 <div class="col-8 col-xs-12 mb-3">
                    <label asp-for="Descricao" class="control-label">DESCRIÇÃO</label>
                    <input asp-for="Descricao" class="form-control" />
                </div>
                <div class="col-12 col-xs-12">
                    <div class="linhaSolida"></div>
                </div>
                <div class="col-12 mb-3" style="text-align: right" >
                    <button type="submit" class="btn btn-primary">
                        <span class="tf-icons bx bx-search"></span>&nbsp; Buscar
                    </button>
                    <a asp-action="create" class="btn btn-secondary">
                        <span class="tf-icons bx bx-plus"></span>&nbsp; Novo
                    </a>                      
                    <a asp-action="" asp-controller="" class="btn btn-secondary">
                        <span class="tf-icons bx bx-arrow-back"></span>&nbsp; Voltar
                    </a>
                </div>
            </div>
        </form >
        <div class="table-responsive">
            <table class="table">
                <thead class="table-dark">
                    <tr>
                        <th colspan="3">
                            NOME
                        </th>
                        <th colspan="8" >
                            DESCRIÇÃO
                        </th>
                        <th colspan="1" style="max-width: 20px">
                            AÇÕES
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Servicos != null && Model.Servicos.Any())
                    {
                        @foreach (var item in Model.Servicos) 
                        {
                            <tr>
                                <td colspan="3">
                                    @Html.DisplayFor(modelItem => item.Nome)
                                </td>
                                <td colspan="8">
                                    @Html.DisplayFor(modelItem => item.Descricao)
                                </td>
                                <td  colspan="1">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-default">
                                        <span class="tf-icons bx bx-edit"></span>
                                    </a>     
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" style="text-align:center">
                                Nenhum resultado encontrado
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

