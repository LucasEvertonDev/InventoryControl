@{
    Layout = "~/Views/Shared/_LayoutBasic.cshtml";
}
<head> 
    

    <link rel="stylesheet" href="~/lib/fullcalendar/main.css" />
    <script src="~/js/moment.js"></script>
    <script src="~/lib/fullcalendar/main.min.js"></script>
    <script src="~/lib/fullcalendar//locales-all.min.js"></script>
    <style>
        .fc-scrollgrid-sync-table > tbody > tr{
            height: 160px;
        }

        .fc-view-harness-active{
            height: 1000px !important;
        }
    </style>
    <script type="text/javascript">
        //$(document).ready(function () {
        //    $('#calendar').fullCalendar({
        //            header: {
        //                left: 'title',
        //                center: 'prev,next today',
        //                right: 'month,listWeek',
        //            },

        //            defaultDate:new Date(),
        //            navLinks: true, // can click day/week names to navigate views
        //            editable: false,
        //            eventLimit: true, // allow "more" link when too many events
        //            events: "Atendimentos/GetEvents",

        //            eventRender: function(eventObj, $el) {
        //                $el.popover({
        //                    title: eventObj.tooltip,
        //                    content: eventObj.descricao,
        //                    trigger: 'hover',
        //                    placement: 'top',
        //                    container: 'body'
        //                });
        //            },

        //            eventClick: function (calEvent, jsEvent, view) {

        //                //var id = calEvent.id;
        //                //var consultaTarefa = "false";
        //                //var url = '@Url.Action("RecuperarItemEventoProtocolo", "EventoProtocolo", new { id = "_id_", consultaTarefa = "_c_" })';
        //                //url = url.replace('_id_', id);
        //                //window.location.href = url.replace('_c_', consultaTarefa);

        //            },
        //        });
        //});

         document.addEventListener('DOMContentLoaded', function() {
              var calendarEl = document.getElementById('calendar');

              var calendar = new FullCalendar.Calendar(calendarEl, {
                themeSystem: "sandstone",
                //defaultDate:new Date(),
                navLinks: false, // can click day/week names to navigate views
                editable: false,
                locale:'pt-br',
                //eventLimit: true, // allow "more" link when too many events
                headerToolbar: {
                    center: 'title',
                    left: 'prev,next today',
                    right: 'dayGridMonth,timeGridWeek'

                },
                dayMaxEvents: 4, // allow "more" link when too many events
                events: "Atendimentos/GetEvents",
                eventTimeFormat: { // like '14:30:00'
                    hour: '2-digit',
                    minute: '2-digit',
                    hour12: false
                }
              });

              calendar.render();
        });
    </script>

</head>
<div class="x_panel">
    <div id='calendar' class="col-lg-12 col-xs-12"></div>
</div>

